# Личный кабинет заочного студента (MVP)

> **Проект для портфолио будущего data engineer**  
> Реализован с акцентом на **качественную архитектуру данных**, **безопасность**, **полную прозрачность кода** и **реальное применение**.

🔗 **Демо (Render):** [https://student-cabinet-mvp.onrender.com](https://student-cabinet-mvp.onrender.com)

---

## 🎯 Возможности MVP

### 👤 Для студента
- ✉️ Отправка учебных работ в электронном виде (с дедлайнами)  
- 📊 Просмотр успеваемости по предметам (оценки, зачёты, статусы)  
- 👩‍🏫 Отображение имён преподавателей по каждому предмету  
- 📱 Полностью адаптивный интерфейс — удобно с мобильного устройства  
- 🔒 Вход по уникальному номеру зачётки (`student_id`)

### 👨‍🏫 Для преподавателя
- 👁️ Просмотр всех отправленных студентами работ по своим предметам  
- 📥 Скачивание файлов работ  
- ✏️ Выставление оценок (0–100) или текстовых статусов (`зачёт`, `не сдано`, `не допущен`)  
- 🔑 Вход по уникальному ID преподавателя (`teacher_id`)

> 💡 Обе роли работают независимо друг от друга — это **единая система с разными представлениями**.

---

## 🛠️ Технологии

| Компонент               | Технология                          |
|------------------------|-------------------------------------|
| Язык                   | Python 3.10+                        |
| Backend                | FastAPI (с типизацией и автоматической документацией) |
| База данных            | SQLite (нормализованная, с внешними ключами) |
| Фронтенд               | Чистый HTML + Bootstrap 5 (адаптивный) |
| Файловое хранилище     | Локальная файловая система с иерархической структурой |
| Деплой                 | Render (бесплатный хостинг)         |
| Управление зависимостями | Виртуальное окружение (`venv`)     |

---

## 🗃️ Архитектура данных

### База данных
Реляционная модель с соблюдением целостности и уникальности:

- `students` — студенты с ФИО и уникальным `student_id`  
- `teachers` — преподаватели с ФИО и `teacher_id`  
- `subjects` — учебные дисциплины  
- `subject_teachers` — связь многие-ко-многим (кто ведёт предмет)  
- `assignments` — задания с дедлайнами  
- `submissions` — отправленные работы (с привязкой к файлу)  
- `grades` — итоговая успеваемость по предмету  

✅ Все связи через `FOREIGN KEY`  
✅ Ограничения: `UNIQUE`, `NOT NULL`, `CHECK`  
✅ Внешние ключи включены явно (`PRAGMA foreign_keys = ON`)

### Файловое хранилище
Файлы **не хранятся в БД** — только пути. Структура:

Структура:  
`storage/submissions/{student_id}/{assignment_id}/{timestamp}_{filename}`  


Пример:  
`storage/submissions/1/2/20260101_200000_lab1.py`

✅ Безопасные имена (фильтрация спецсимволов)  
✅ Изоляция по студентам и заданиям

> ⚠️ **Важно**: На бесплатном тарифе Render файловая система **ephemeral** — данные сбрасываются после перезапуска.
