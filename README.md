# Личный кабинет заочного студента (MVP)

> Проект для портфолио будущего развития навыков по специальности **инженер данных**  
> Реализован с акцентом на **качественную архитектуру данных**, **безопасность** и **полную прозрачность кода**.

---

## 🎯 Цель

Создать минимально жизнеспособный личный кабинет студента заочной формы обучения, который позволяет:

- ✉️ Отправлять учебные работы преподавателю в электронном виде  
- 📊 Просматривать успеваемость по предметам (оценки, зачёты, статусы)  
- 📅 Получать задания с дедлайнами  
- 📱 Удобно использовать с мобильного устройства  

Проект разрабатывается **не как временный скрипт**, а как образцовая система с продуманной структурой — именно так, как ожидается от специалиста в области data engineering.

---

## 🛠️ Технологии

| Компонент               | Технология                          |
|------------------------|-------------------------------------|
| Язык                   | Python 3.10+                        |
| Backend                | FastAPI (с типизацией и документацией) |
| База данных            | SQLite (готов к миграции на PostgreSQL) |
| Фронтенд               | Чистый HTML + Bootstrap 5 (адаптивный) |
| Файловое хранилище     | Локальная файловая система          |
| Управление зависимостями | Виртуальное окружение (`venv`)     |

> 💡 Выбор SQLite обусловлен MVP-подходом. При этом схема БД **полностью нормализована**, содержит внешние ключи, ограничения и соответствует лучшим практикам проектирования.

---

## 🗃️ Архитектура данных

### База данных
Реляционная модель с соблюдением целостности и уникальности:

- `students` — студенты с ФИО и уникальным `student_id` (учитывает особенности стран СНГ)  
- `subjects` — учебные дисциплины  
- `assignments` — задания с дедлайнами  
- `submissions` — отправленные работы (с привязкой к файлу)  
- `grades` — итоговая успеваемость по предмету  

✅ Все связи через `FOREIGN KEY`  
✅ Ограничения: `UNIQUE`, `NOT NULL`, `CHECK`  
✅ Внешние ключи включены явно (`PRAGMA foreign_keys = ON`)

### Файловое хранилище
Файлы **не хранятся в БД** — только пути.  
Структура:  
`storage/submissions/{student_id}/{assignment_id}/{timestamp}_{filename}`  

Пример:  
`storage/submissions/1/2/20251231_180000_lab1.py`

✅ Безопасные имена (фильтрация спецсимволов)  
✅ Изоляция по студентам и заданиям  
✅ Готово к бэкапу, миграции и масштабированию

---

## 🚀 Быстрый запуск

1. **Клонируй репозиторий**
   ```bash
   git clone https://github.com/ваш-username/student-cabinet-mvp.git
   cd student-cabinet-mvp
